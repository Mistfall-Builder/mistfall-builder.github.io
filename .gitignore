# donnees.js est ce que la page charge réellement (une balise <script>, qui
# marche même en file://). donnees.json n'est qu'une copie du même contenu,
# gardée en local pour les tests hors navigateur : l'envoyer doublerait le
# poids du site pour rien.
donnees.json
